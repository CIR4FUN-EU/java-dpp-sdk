# Mock Repo PostgreSQL Integration Log

- Date: 2026-06-29
- Goal: Integrate the new standalone PostgreSQL Dpp4Fun repository into `dpp-sdk-demo/mock-dpp-repo` as an optional backend mode.
- Scope:
  - preserve existing mock repo HTTP API behavior
  - keep memory mode as the default backend
  - add PostgreSQL backend wiring and tests
  - update demo/mock docs
- Constraints:
  - do not change endpoint paths or response shapes
  - keep validation, merge patch, and fine-granular element logic in the mock service layer
  - do not remove or weaken memory-mode tests
  - do not introduce SDK-wide backend abstractions
- Planned validation:
  - `.\mvnw.cmd -pl dpp-sdk-demo -am test`
  - `.\mvnw.cmd -pl dpp-postgres -am test` if the PostgreSQL module changes
- Implementation summary:
  - added a mock-local `DppRepoBackend` seam so the HTTP service can run against either the existing in-memory store or `Dpp4FunPostgresRepository`
  - kept controller paths, JSON codec usage, validation, JSON Merge Patch, fine-granular element-path logic, and HTTP error mapping in the mock service layer
  - kept memory mode as the default backend and added `dpp.repo.backend=postgres` for PostgreSQL mode
  - added PostgreSQL-backed endpoint tests with Testcontainers without weakening existing memory-mode tests
- Backend design:
  - `InMemoryDppRepoBackend` wraps the existing `InMemoryDppStore`
  - `PostgresDppRepoBackend` wraps `Dpp4FunPostgresRepository`
  - `DppRepoService` now depends on the backend seam instead of directly on the in-memory store
  - PostgreSQL lifecycle events are mapped back into the existing mock `LifecycleEventRecord` response shape
- Validation run:
  - attempted `.\mvnw.cmd -f dpp-sdk-demo\pom.xml -pl mock-dpp-repo -am test`
  - this failed because `mock-dpp-repo` now depends on `dpp4fun-postgres`, which is outside the `dpp-sdk-demo` reactor and was not yet installed in the local repository
  - ran `.\mvnw.cmd -pl dpp-postgres/dpp4fun-postgres,dpp-sdk-demo/mock-dpp-repo -am test`
  - initial failure: Spring Boot tried to auto-configure a JDBC datasource in default memory mode and failed with `Failed to determine a suitable driver class`
  - fix applied: removed JDBC auto-configuration dependency and created the PostgreSQL `DataSource` explicitly only under `dpp.repo.backend=postgres`
  - reran `.\mvnw.cmd -pl dpp-postgres/dpp4fun-postgres,dpp-sdk-demo/mock-dpp-repo -am test`
  - result: `BUILD SUCCESS`
  - memory-mode mock tests passed
  - PostgreSQL module tests passed where non-containerized and skipped containerized integration tests when Docker was unavailable
  - `DppRepoControllerPostgresBackendTest` was skipped because Testcontainers could not find a valid Docker environment in this session
- Notes:
  - no endpoint paths or response bodies were changed
  - no old tests were removed or weakened
  - PostgreSQL-backed mock tests are written to activate only when Docker is available
